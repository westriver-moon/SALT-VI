# Experiment templates

Templates are intentionally not directly runnable. Copy one into an active configuration location, set all placeholder values to validated assets, and then run static and GPU smoke checks before using it for an experiment.

`a3_e4_hpt_l025_template.yaml` requires a topology-compatible A3/E4 initialization checkpoint in `training_weight_init`.
