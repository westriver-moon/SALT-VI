schema_version: 1
experiment_id: qri-fast-search-gpu0-20260821
device: cuda:0
dataset_scope:
  trainval_images: 34167
  identities: 395
  identity_modality_groups: 790
phase_a:
  algorithm: batched_swinir_cached_sam
  swinir_batch_size: 16
  keep_top_regions: 2
  uncertainty: blur_only
  tta_enabled: false
  pose_missing_policy: full_frame_fallback
phase_b:
  algorithm: one_shot_qwen_group_cache
  grouping: identity_modality
  calls_expected: 790
  regions_per_anchor: 2
  worlds_max: 3
  thinking: false
  max_tokens: 1024
output_policy:
  default_image: swinir
  pasd_enabled: false
  rationale: PASD is optional until a larger visual quality audit passes.
optional_pasd:
  process_size: 256
  steps: 8
  views_per_image: 1
  fallback: swinir
  gates:
    min_inside_mean_abs_change: 3.0
    max_outside_mean_abs_change: 0.1
    max_lr_cycle_energy: 0.003
    min_identity_cosine_to_swin: 0.96
