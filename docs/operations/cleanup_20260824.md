# ybj 工作区清理与归档记录（2026-08-24）

## 范围

本轮以 `/home/lab929/ybj/SALT-VI` 为当前研究主仓库，同时整理
`/home/lab929/ybj` 顶层的旧 QRI-v2 验证树、松散脚本和可再生缓存。数据集、模型、
checkpoint、训练输出、实验原始载荷、Git 恢复包和活动 worktree 均不删除。

## 文档结构

- `docs/status/`：唯一当前状态入口；
- `docs/architecture/`：架构审计与重整记录；
- `docs/plans/`：设计、修复和后续计划；
- `docs/operations/`：运行、归档和清理规范；
- `docs/history/`：已结束实验、失败与负面结果叙述；
- `docs/reference/`：结构化表字段和稳定接口参考；
- `reports/`：仅保存结构化或机器生成证据。

根 README 和组件 README 作为代码入口保留，但不再复制当前状态或排行榜。

## 归档和删除策略

- 已被 SALT-VI 吸收且工作树干净的 QRI-v2 验证仓库、对应 bundle 和顶层辅助脚本，
  移入 `/home/lab929/ybj/archive/qri-v2-localized-legacy-20260824/`；
- 临时 RRF 诊断目录和旧 worktree 压缩包按来源移入 `archive/` 或 `git-archives/`；
- `__pycache__`、`.pytest_cache`、`.mypy_cache`、`.ruff_cache`、
  `.ipynb_checkpoints`、未跟踪 `*.egg-info` 和空临时文件属于可再生产物，直接删除；
- 本轮开始时已有的 QRI V4 改动已保留并纳入当前分支；活动 Stage-B worktree、PASD
  用户修改、实验输出、模型、数据集和恢复性 Git 归档保持原样。

## 执行结果

- 77 个缓存/安装产物目录（约 4.4 MB）已删除，复核残留为 0；
- QRI-v2、临时诊断、旧启动脚本和恢复包均已写入带 README 的日期归档；
- 一个运行 6 天、无训练子进程且匹配表达式错误的旧 watcher 已停止。
- 核心、PASD、Qwen V4 与 semantic-imagination 测试共 185 passed、1 skipped。

## 恢复

归档对象均为同文件系统移动，可直接移回原路径。缓存不归档，可由 Python、pytest 或
安装命令重新生成。
