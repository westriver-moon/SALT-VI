[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "salt-semantic-imagination"
version = "0.6.0"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6"]

[tool.setuptools.packages.find]
where = ["."]
include = ["semantic_imagination*"]
