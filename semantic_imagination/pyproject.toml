[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "salt-semantic-imagination"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["semantic_imagination"]
